package IntroducingMethods.ReturningAValue;
// Now, volume() returns the volume of a box.

public class Box {
    double width;
    double height;
    double depth;

    // compute and return volume
    double volume() {
        return width * height * depth;
    }
}
